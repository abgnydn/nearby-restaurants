import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'nearby-restaurants-test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
